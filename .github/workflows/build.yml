name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      # 번들 조립과 ad-hoc 서명까지 로컬과 같은 경로로 확인한다.
      - run: make build
